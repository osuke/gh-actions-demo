name: Hello, World!
on:
  push:
    # Specify branch name
    branches:
      - master
    tags:
      - "!*"

jobs:
  # Job ID
  build:
    # Job name
    name: Greetings
    # Virtual machine
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
      - run: npm run test
